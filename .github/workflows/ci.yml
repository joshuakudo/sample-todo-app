name: CI/CD Pipeline for React App

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up Node.js environment
      - name: Set Up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Step 3: Install dependencies
      - name: Install Dependencies
        run: npm install

      # Step 5: Build the React application
      - name: Build Application
        run: npm run build

      # Step 6: Set up Docker for containerization
      - name: Set Up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Step 7: Log in to Docker Registry (GitHub Container Registry in this case)
      - name: Log In to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Step 8: Build and push the Docker image
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:latest

      # Step 9: Send Email Notification
      - name: Send Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: monleonjoshuaa6516@gmail.com
          password: nkku qoyc drav guic
          subject: "GitHub Actions: React App CI/CD Pipeline Status"
          body: |
            The CI/CD pipeline for your React app has been successfully completed.
            Details:
            - Repository: ${{ github.repository }}
            - Branch: ${{ github.ref_name }}
            - Commit: ${{ github.sha }}
          to: monleonjoshua00@gmail.com
          from: monleonjoshuaa6516@gmail.com
